# UI Docs

`docs/ui/`는 관리자 화면의 레이아웃, 사용 시나리오, 검수 플로우를 정리하는 문서를 담는다.

## 문서 목록

- `admin_backoffice.md`
  - React Admin 백오피스의 화면 구조, UX 원칙, 주요 동선 설명

## 참고

실제 화면 구현은 `frontend/src/`에 있고, 빌드 산출물은 `backend/src/main/resources/static/react/`로 배포되어 `/admin/*` 경로에서 서빙된다.
